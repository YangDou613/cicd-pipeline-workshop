name: Deploy

on:
  pull_request:
    branches: main
    types: [closed]

jobs:
  deploy-web:
    runs-on: ubuntu-latest
    steps:
      - if: ${{ github.event.pull_request.merged }}
	run: echo "User name: ${{ secrets.USER_NAME }}"
